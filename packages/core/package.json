{
  "name": "@search-audit/core",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@search-audit/mcp": "workspace:*",
    "@search-audit/extractors": "workspace:*",
    "@search-audit/judge": "workspace:*",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "tsup": "^8.0.1",
    "typescript": "^5.3.3"
  }
}
