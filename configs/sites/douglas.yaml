# Douglas.de specific configuration

site:
  url: "https://www.douglas.de"
  locale: "de-DE"

  search:
    input_selectors:
      # Douglas search input
      - 'input[data-testid="typeahead-search-input"]'
      - 'input[id="typeAheadSearchField"]'
      - 'input[name="search"]'
      - 'input[type="search"]'
      - 'input[aria-label*="Suche" i]'
      - 'input[aria-label*="Search" i]'
      - 'input[placeholder*="Suche" i]'
      - 'input[placeholder*="Search" i]'
    submit_strategy: "enter"
    submit_selector: null
    search_url_template: "https://www.douglas.de/de/search?q={query}"
    use_intelligent_fallback: true
    intelligent_detection_model: "gpt-4o-mini"

  results:
    item_selectors:
      - '[data-testid="product-card"]'
      - 'div[class*="product-card"]'
      - 'article[class*="product"]'
      - 'div[class*="ProductCard"]'
    title_selectors:
      - '[data-testid="product-title"]'
      - 'h3'
      - 'h2'
      - 'a[class*="product-name"]'
      - 'div[class*="product-name"]'
    url_attr: 'href'
    snippet_selectors:
      - 'span[class*="brand"]'
      - 'div[class*="brand"]'
      - 'p[class*="description"]'
      - 'span'
    price_selectors:
      - '[data-testid="product-price"]'
      - 'span[class*="price"]'
      - 'div[class*="price"]'
    image_selectors:
      - 'img[class*="product"]'
      - 'img'

  modals:
    close_text_matches:
      - "akzeptieren"
      - "alle akzeptieren"
      - "accept"
      - "agree"
      - "weiter"
      - "continue"
      - "schliessen"
      - "close"
      - "ablehnen"
      - "reject"
      - "alle ablehnen"
      - "nur notwendige"
    max_auto_clicks: 3
    wait_after_close_ms: 500

run:
  top_k: 10
  viewport_width: 1366
  viewport_height: 900
  network_idle_ms: 3000
  post_submit_ms: 2000
  headless: true
  throttle_rps: 0.5
  seed: 42
  browser_backend: playwright

llm:
  provider: "openrouter"
  model: "openai/gpt-4o"
  max_tokens: 2000
  temperature: 0.2
  system_prompt: null
  base_url: "https://openrouter.ai/api/v1"
  api_key: null  # Set in .env as OPENROUTER_API_KEY

report:
  formats:
    - "md"
    - "html"
  out_dir: "./runs"
