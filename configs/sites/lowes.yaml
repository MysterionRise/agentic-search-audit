# Lowes.com specific configuration

site:
  url: "https://www.lowes.com"
  locale: "en-US"

  search:
    input_selectors:
      - '#search-query'
      - 'input[type="search"]'
      - 'input[aria-label*="Search" i]'
      - 'input[placeholder*="Search" i]'
    submit_strategy: "enter"
    submit_selector: null
    search_url_template: "https://www.lowes.com/search?searchTerm={query}"
    use_intelligent_fallback: true

  results:
    item_selectors:
      - '[data-selector="prd-card"]'
      - 'div[class*="product-card"]'
      - '[data-testid*="product"]'
      - '.plp-card'
    title_selectors:
      - 'a[data-selector="prd-ttl"]'
      - '[data-selector="prd-ttl"]'
      - '.product-title a'
      - 'h3 a'
    url_attr: 'href'
    snippet_selectors:
      - '[data-selector="prd-desc"]'
      - '.product-description'
      - 'p'
    price_selectors:
      - '[data-selector="prd-prc"]'
      - 'div[class*="price"]'
      - 'span[class*="price"]'
    image_selectors:
      - '[data-selector="prd-img"] img'
      - '.product-image img'
      - 'img'

  modals:
    close_text_matches:
      - "accept"
      - "agree"
      - "close"
      - "dismiss"
      - "no thanks"
      - "got it"
      - "not now"
    max_auto_clicks: 3
    wait_after_close_ms: 500

run:
  top_k: 10
  viewport_width: 1366
  viewport_height: 900
  network_idle_ms: 3000
  post_submit_ms: 2000
  headless: true
  throttle_rps: 0.5
  seed: 42
  # Lowe's uses bot detection; undetected-chromedriver recommended.
  browser_backend: undetected

llm:
  provider: "openrouter"
  model: "openai/gpt-4o"
  max_tokens: 2000
  temperature: 0.2
  system_prompt: null
  base_url: "https://openrouter.ai/api/v1"
  api_key: null  # Set in .env as OPENROUTER_API_KEY

report:
  formats:
    - "md"
    - "html"
  out_dir: "./runs"
