# Otto.de specific configuration

site:
  url: "https://www.otto.de"
  locale: "de-DE"

  search:
    input_selectors:
      # Otto search input
      - 'input[id="searchFieldInput"]'
      - 'input[name="search"]'
      - 'input[data-testid="search-input"]'
      - 'input[type="search"]'
      - 'input[aria-label*="Suche" i]'
      - 'input[aria-label*="Search" i]'
      - 'input[placeholder*="Suche" i]'
      - 'input[placeholder*="Search" i]'
    submit_strategy: "enter"
    submit_selector: null
    search_url_template: "https://www.otto.de/suche/{query}/"
    use_intelligent_fallback: true
    intelligent_detection_model: "gpt-4o-mini"

  results:
    item_selectors:
      - '[data-testid="product-tile"]'
      - 'article[class*="product"]'
      - 'div[class*="product-tile"]'
      - 'div[class*="productTile"]'
    title_selectors:
      - '[data-testid="product-title"]'
      - 'h3'
      - 'h2'
      - 'a[class*="product-title"]'
      - 'span[class*="title"]'
    url_attr: 'href'
    snippet_selectors:
      - 'span[class*="brand"]'
      - 'p[class*="description"]'
      - 'span[class*="subtitle"]'
      - 'span'
    price_selectors:
      - '[data-testid="product-price"]'
      - 'span[class*="price"]'
      - 'div[class*="price"]'
    image_selectors:
      - 'img[class*="product"]'
      - 'img'

  modals:
    close_text_matches:
      - "akzeptieren"
      - "alle akzeptieren"
      - "accept"
      - "agree"
      - "weiter"
      - "continue"
      - "schliessen"
      - "close"
      - "ablehnen"
      - "reject"
      - "alle ablehnen"
      - "nur notwendige"
      - "necessary only"
    max_auto_clicks: 3
    wait_after_close_ms: 500

run:
  top_k: 10
  viewport_width: 1366
  viewport_height: 900
  network_idle_ms: 3000
  post_submit_ms: 2000
  headless: true
  throttle_rps: 0.5
  seed: 42
  # Otto uses bot detection; undetected-chromedriver recommended.
  browser_backend: undetected

llm:
  provider: "openrouter"
  model: "openai/gpt-4o-mini"
  max_tokens: 2000
  temperature: 0.2
  system_prompt: null
  base_url: "https://openrouter.ai/api/v1"
  api_key: null  # Set in .env as OPENROUTER_API_KEY

report:
  formats:
    - "md"
    - "html"
  out_dir: "./runs"
