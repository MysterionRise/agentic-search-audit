# IKEA (Swedish site) specific configuration

site:
  url: "https://www.ikea.com/se/sv/"
  locale: "sv-SE"

  search:
    input_selectors:
      # IKEA search input
      - 'input[id="ikea-search-input"]'
      - 'input[data-testid="search-field"]'
      - 'input[name="q"]'
      - 'input[type="search"]'
      - 'input[aria-label*="Sök" i]'
      - 'input[aria-label*="Search" i]'
      - 'input[placeholder*="Sök" i]'
      - 'input[placeholder*="Search" i]'
    submit_strategy: "enter"
    submit_selector: null
    search_url_template: "https://www.ikea.com/se/sv/search/?q={query}"
    use_intelligent_fallback: true
    intelligent_detection_model: "gpt-4o-mini"

  results:
    item_selectors:
      - '[data-testid="plp-product-card"]'
      - 'div[class*="product-card"]'
      - 'div[class*="serp-grid__item"]'
      - 'article'
    title_selectors:
      - '[class*="product-compact__name"]'
      - 'span[class*="product-name"]'
      - 'h3'
      - 'h2'
    url_attr: 'href'
    snippet_selectors:
      - 'span[class*="product-compact__description"]'
      - 'span[class*="description"]'
      - 'p'
      - 'span'
    price_selectors:
      - '[class*="product-compact__price"]'
      - 'span[class*="price"]'
      - 'div[class*="price"]'
    image_selectors:
      - 'img[class*="product"]'
      - 'img'

  modals:
    close_text_matches:
      - "acceptera"
      - "acceptera alla"
      - "accept"
      - "agree"
      - "fortsätt"
      - "continue"
      - "stäng"
      - "close"
      - "avvisa"
      - "reject"
      - "neka"
      - "accept all"
    max_auto_clicks: 3
    wait_after_close_ms: 500

run:
  top_k: 10
  viewport_width: 1366
  viewport_height: 900
  network_idle_ms: 3000
  post_submit_ms: 2000
  headless: true
  throttle_rps: 0.5
  seed: 42
  browser_backend: playwright

llm:
  provider: "openrouter"
  model: "openai/gpt-4o"
  max_tokens: 2000
  temperature: 0.2
  system_prompt: null
  base_url: "https://openrouter.ai/api/v1"
  api_key: null  # Set in .env as OPENROUTER_API_KEY

report:
  formats:
    - "md"
    - "html"
  out_dir: "./runs"
